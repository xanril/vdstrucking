@model VDSTrucking.Models.HelperList

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>HelperList</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="HelperListID" />
        <div class="form-group">
            <label asp-for="TripID" class="control-label col-md-2"></label>
            <div class="col-md-10">
                <select asp-for="TripID" class="form-control" asp-items="ViewBag.TripID"></select>
                <span asp-validation-for="TripID" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
